# Production environment — automatically used by Vite during `npm run build`
# This tells the client where the deployed backend API lives.
# The axios instance uses this as baseURL, so it must end with /api
VITE_API_BASE_URL=https://tasklab-beta.vercel.app/api
